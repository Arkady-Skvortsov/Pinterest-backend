import { CommentsInterceptor } from './comments.interceptor';

describe('CommentsInterceptor', () => {
  it('should be defined', () => {
    expect(new CommentsInterceptor()).toBeDefined();
  });
});
